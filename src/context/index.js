import {createContext} from "react";

export const HeaderIconsContext = createContext('');

export const HeaderProfileIconsContext = createContext('');

export const SidebarRowContext = createContext('');

export const StoriesContext = createContext('')
